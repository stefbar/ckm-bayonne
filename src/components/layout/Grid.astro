---
interface Props {
  cols?: 2 | 3 | 4;
  class?: string;
}

const {
  cols = 3,
  class: className = "",
} = Astro.props;
---

<div class:list={[
  "l-grid",
  `l-grid--${cols}`,
  className,
]}>
  <slot />
</div>